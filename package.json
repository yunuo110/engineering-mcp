{
  "name": "engineering-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Engineering MCP V1 coordination ledger",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^24.3.0",
    "typescript": "~5.9.2",
    "vitest": "^4.1.11"
  }
}
